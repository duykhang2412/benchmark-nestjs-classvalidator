{
  "name": "@apps/benchmark-nestjs-classvalidator",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "common": "pnpm --filter @packages/common",
    "mongodb-connector": "pnpm --filter @packages/mongodb-connector",
    "redis-connector": "pnpm --filter @packages/redis-connector",
    "packages:build": "pnpm run common build && pnpm run mongodb-connector build && pnpm run redis-connector build "
  }
}